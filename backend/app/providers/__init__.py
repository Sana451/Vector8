"""
Provider layer.

Domain-oriented provider protocols, adapters and the registry that binds them
to configuration. Import submodules directly to avoid import cycles:

    from app.providers.base import RoutingProvider
    from app.providers.registry import registry
"""
